{
  "name": "sails-generate-api-test",
  "version": "0.10.0",
  "description": "sails generate api-test",
  "main": "Generator.js",
  "scripts": {
    "test": "mocha"
  },
  "keywords": [
    "api-test",
    "generator",
    "sails",
    "generate",
    "plugin"
  ],
  "author": "tbarho",
  "license": "MIT",
  "dependencies": {
    "lodash": "^3.3.1",
    "merge-defaults": "^0.2.1",
    "pluralize": "^1.1.2"
  },
  "devDependencies": {
    "sails-generate": "*",
    "reportback": "*",
    "fs-extra": "*"
  },
  "sailsGenerator": {
    "type": "api-test",
    "behavior": "overrides `sails generate api-test`",
    "sailsVersion": "~0.10.0"
  }
}
